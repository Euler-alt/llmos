import React from 'react';
import SystemMonitor from './SystemMonitor';

const MonitorTab = ({ darkMode }) => {
  return (
    <SystemMonitor darkMode={darkMode} />
  );
};

export default MonitorTab;